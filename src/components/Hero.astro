---

---

<section id="inicio" class="hero">
  <div class="overlay"></div>
  <div class="container hero-content">
    <span class="subtitle">Soluciones Integrales</span>
    <h1>
      Construcción y Mantenimiento <br />
      <span class="highlight">Especializado</span>
    </h1>
    <p>
      Una empresa comprometida en brindar soluciones para el sector industrial y
      de servicios, utilizando las herramientas adecuadas para la ejecución,
      administración y control de los trabajos.
    </p>

    <div class="hero-btns">
      <a href="#servicios" class="btn">Nuestros Servicios</a>
      <a href="#contacto" class="btn btn-outline">Contáctanos</a>
    </div>

    <div class="hero-stats">
      <div class="stat">
        <strong>RFC</strong>
        <span>KMI1909174X6</span>
      </div>
      <div class="stat">
        <strong>Ubicación</strong>
        <span>Santa Bárbara, SLP</span>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    background-image: url("https://images.unsplash.com/photo-1581091226825-a6a2a5aee158?q=80&w=2670&auto=format&fit=crop"); /* Industrial Background */
    background-size: cover;
    background-position: center;
  }

  .overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, #000000 0%, rgba(0, 0, 0, 0.7) 100%);
  }

  .hero-content {
    position: relative;
    z-index: 1;
    max-width: 1280px;
  }

  .subtitle {
    color: var(--primary);
    text-transform: uppercase;
    letter-spacing: 3px;
    font-weight: 700;
    font-size: 0.9rem;
    margin-bottom: 1rem;
    display: block;
  }

  h1 {
    font-size: 4.5rem;
    margin-bottom: 1.5rem;
    line-height: 1.1;
    color: white;
  }

  .highlight {
    color: transparent;
    -webkit-text-stroke: 1px white;
    position: relative;
  }

  .highlight::after {
    content: "Especializado";
    position: absolute;
    left: 0;
    top: 0;
    color: var(--primary);
    width: 0%;
    overflow: hidden;
    white-space: nowrap;
    transition: 1s ease-in-out;
    border-right: 2px solid var(--primary);
  }

  .hero:hover .highlight::after {
    width: 100%;
  }

  p {
    font-size: 1.2rem;
    max-width: 600px;
    margin-bottom: 2.5rem;
    color: #eee;
  }

  .hero-btns {
    display: flex;
    gap: 1rem;
  }

  .btn-outline {
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.3);
    color: white;
    padding: 12px 30px;
    font-weight: 700;
    text-transform: uppercase;
  }

  .btn-outline:hover {
    background: white;
    color: black;
  }

  .hero-stats {
    margin-top: 4rem;
    display: flex;
    gap: 3rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding-top: 2rem;
  }

  .stat strong {
    display: block;
    color: var(--primary);
    text-transform: uppercase;
    font-size: 0.8rem;
    margin-bottom: 0.3rem;
  }

  .stat span {
    font-size: 1.1rem;
    font-weight: 500;
  }

  @media (max-width: 768px) {
    h1 {
      font-size: 3rem;
    }
    .hero-stats {
      display: none;
    }
  }
</style>
